let cadena = document.querySelector("div.cuadro-glosa>p").innerText;
    console.warn(cadena)

    /*let regex = /[(]/g
    let found = cadena.match(regex)
    let transform = found.html('(<i class="fas fa-question-circle text-primary"></i>')
    console.log(transform) */
